import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  signupTitle: {
    color: 'red',
  },
  bold: {
    fontWeight: 'bold',
  },
  submit: {
    height: 50,
    marginTop: 15,
    marginBottom: 15,
    color: '#FFFFFF',
    fontWeight: 'bold',
  },
  policy: {
    fontSize: 12,
  },
});

export default useStyles;
